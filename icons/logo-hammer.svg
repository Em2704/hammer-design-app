<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" role="img" aria-label="Hammering Assessment Tools">
  <!-- Standalone form of the glyph in icon.html, which the PWA icons were generated from.
       Keep the two in sync if the mark ever changes. -->
  <rect width="100" height="100" rx="18" fill="#10323d"/>
  <g transform="translate(50 50) scale(0.66) translate(-50 -50)">
    <!-- handle -->
    <rect x="45" y="40" width="10" height="46" rx="3" fill="#139187"/>
    <!-- head -->
    <rect x="22" y="22" width="56" height="22" rx="5" fill="#fcb03c"/>
    <!-- struck face (darker end) -->
    <rect x="22" y="22" width="12" height="22" rx="5" fill="#c9852a"/>
    <!-- gauge tick baseline (spec-sheet motif) -->
    <g fill="#8899a8">
      <rect x="30" y="90" width="3" height="6" rx="1"/>
      <rect x="42" y="90" width="3" height="6" rx="1"/>
      <rect x="54" y="90" width="3" height="6" rx="1"/>
      <rect x="66" y="90" width="3" height="6" rx="1"/>
    </g>
  </g>
</svg>
